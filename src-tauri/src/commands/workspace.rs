//! Workspace settings commands — NoEnding Home, default workspace, reconcile.
//!
//! Owned by `Agent A` (方案 §16) for Home and by `Agent B` (方案 §17) for the
//! registry. Wave 0 leaves this file empty on purpose: the frozen surface is
//! `get_workspace_settings` / `set_noending_home` (方案 §11), whose result shape
//! is pinned in `src/types.ts` so the frontend agent can build against it.
